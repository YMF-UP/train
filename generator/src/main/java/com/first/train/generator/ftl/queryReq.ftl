package com.first.train.${module}.req;

import com.first.train.common.req.PageReq;

public class ${Domain}QueryReq extends PageReq {

    @Override
    public String toString() {
        return "${Domain}QueryReq{" +
                "} " + super.toString();
    }
}
